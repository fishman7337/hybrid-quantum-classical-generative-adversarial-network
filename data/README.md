# `data`

Local data belongs here and is not committed. The MNIST dataset is normally
downloaded by TensorFlow/Keras into the user's local cache, so this repository
does not need to store raw MNIST files.

Use:

- `raw/` for original downloaded or externally supplied data.
- `processed/` for derived datasets used by experiments.
