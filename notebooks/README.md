# `notebooks`

Jupyter notebooks live here.

Current notebook:

- `Quantum-GAN.ipynb`: exploratory GAN notebook for binary MNIST, now organized
  with markdown sections and corrected metric-preprocessing guidance.
- `legacy/DELE_CA2_C.ipynb`: legacy notebook preserved from the original
  GitHub repository for provenance.

For reusable logic, prefer moving stable code into `src/hqcgan/` and importing
it from notebooks.
