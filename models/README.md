# `models`

Store local model checkpoints here during experiments. Checkpoints are ignored
by git because they can be large and environment-specific.

For publishable results, record the model configuration, random seed, training
run, and evaluation metrics alongside the artifact.
